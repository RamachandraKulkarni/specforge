Identify conditional visibility from these screen variants.

SCREEN VARIANTS:
{{variants}}

Respond:
{"visibility_rules": [{"target_type": "column | button | section | grid", "target_id": "string", "visible_when": "condition expression", "hidden_when": "condition expression", "trigger": "filter_change | tab_switch | row_selection | state_change", "confidence": 0.0-1.0}]}

Perform a deep-dive analysis on this individual column that had low confidence in the initial pass.

COLUMN DATA:
{{column}}

FULL TABLE CONTEXT:
{{table}}

Provide a complete, high-confidence column specification matching the standard column schema.
Include all fields: col_key, header_label, data_type, format, editable, sortable, filterable, filter_type, width_px, validation, source_api_field, renderer, editor, confidence (must be >= 0.85).
